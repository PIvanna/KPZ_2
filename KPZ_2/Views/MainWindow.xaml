<Window x:Class="MedTeleHelp.WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MedTeleHelp.WPF.ViewModels"
        xmlns:converters="clr-namespace:MedTeleHelp.WPF.Converters"
        xmlns:userControls="clr-namespace:MedTeleHelp.WPF.UserControls"
        mc:Ignorable="d"
        Title="MedTeleHelp - Панель пацієнта" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <converters:StatusToBrushConverter x:Key="StatusToBrushConverter"/>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,0,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Список лікарів" Style="{StaticResource HeaderTextStyle}" VerticalAlignment="Center" Margin="0,0,20,0"/>
                <Button Content="+" Command="{Binding AddDoctorCommand}" Width="30" Height="30" ToolTip="Додати лікаря" Background="Green"/>
                <Button Content="-" Command="{Binding DeleteDoctorCommand}" Width="30" Height="30" Margin="5,0,0,0" ToolTip="Видалити вибраного" Background="Red"/>
            </StackPanel>

            <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="1" Padding="5" Margin="0,0,0,10" CornerRadius="3">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Фільтр (Stored Proc): Рейтинг > " VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Text="{Binding FilterRating}" Width="40" VerticalContentAlignment="Center" Margin="0,0,10,0"/>
                    <Button Content="Фільтрувати" Command="{Binding FilterDoctorsCommand}" Margin="0,0,5,0" Background="#FF007ACC"/>
                    <Button Content="Скинути" Command="{Binding ResetFilterCommand}" Background="Gray"/>
                </StackPanel>
            </Border>
            
            <DataGrid Grid.Row="2" ItemsSource="{Binding Doctors}"
                      SelectedItem="{Binding SelectedDoctor}"
                      AutoGenerateColumns="False" IsReadOnly="True"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ПІБ" Binding="{Binding FullName}" Width="*"/>
                    <DataGridTextColumn Header="Спеціалізація" Binding="{Binding Specialization}" Width="*"/>
                    <DataGridTextColumn Header="Рейтинг" Binding="{Binding Rating}" Width="Auto"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock Grid.Row="3" Text="Мої записи на прийом" Style="{StaticResource HeaderTextStyle}" Margin="0,20,0,10"/>
            
            <DataGrid Grid.Row="4" ItemsSource="{Binding Appointments}"
                      AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Скасувати запис" 
                                  Command="{Binding DeleteAppointmentCommand}" 
                                  CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=ContextMenu}, Path=PlacementTarget.SelectedItem}"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Лікар" Binding="{Binding DoctorFullName}" Width="*"/>
                    <DataGridTextColumn Header="Дата та час" Binding="{Binding AppointmentTime, StringFormat='dd.MM.yyyy HH:mm'}" Width="*"/>
                    <DataGridTemplateColumn Header="Статус" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Status}" FontWeight="Bold" Foreground="{Binding Status, Converter={StaticResource StatusToBrushConverter}}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <StackPanel Grid.Column="1" Margin="10,0,0,0">
            <TextBlock Text="Деталі лікаря" Style="{StaticResource HeaderTextStyle}"/>
            
            <userControls:DoctorProfileCard Doctor="{Binding SelectedDoctor}" Margin="0,0,0,20"/>
            
            <Button Content="Записатись на прийом" Command="{Binding OpenBookingWindowCommand}" Height="40" FontSize="16"/>
        </StackPanel>
    </Grid>
</Window>